'use strict';

angular.module('app.controllers').
    controller('NodeModulesController', function($scope, $routeParams, npmService) {
    npmService.find($routeParams.name).success(function(module) {
        $scope.module = module;
    });
});
