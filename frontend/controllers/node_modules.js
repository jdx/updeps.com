'use strict';

app.controller('NodeModulesController', function($scope, $stateParams, NpmService) {
    NpmService.find($stateParams.name).success(function(module) {
        $scope.module = module;
    });
});
